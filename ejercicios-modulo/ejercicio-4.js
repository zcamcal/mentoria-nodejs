/**
 *
Ejercicio 4: Determinar si un año es bisiesto

Enunciado: Escribe una función que reciba un año como parámetro y determine si ese año es bisiesto. Un año es bisiesto si cumple con las siguientes condiciones:
    Es divisible entre 4.
    No es divisible entre 100, a menos que también sea divisible entre 400, esto quiere decir que un anio que sea divisible por 100 tambien debe serlo por 400 para ser bisiesto como el anio 1700 por ejemplo.
 *
 */

/**
 * @param {number} anio el anio como tal por ejemplo 2024
 *
 * @returns {boolean} retorna true si el anio es bisiesto
 * @example
 * esAnioBisiesto(2024) = true; el anio 2024 es bisiesto por que 2024 es divisible entre 4, si no es bisiesto entonces false
 *
 */
export function esAnioBisiesto(anio) {
  // tu codigo aqui :)
  throw new Error("no implementada la funcion!") //eliminar cuando se vaya a implementar
}
