/*
Enunciado: Escribe una función que reciba un número entero y determine si es par o impar. La función debe devolver un mensaje indicando si el número es par o impar.
*/

/**
 * Esta funcion nos permite determinar si un numero es par o impar
 * @param {number} numero 
 *
 * @returns {boolean} true si es par, false si no fue par
 * @example
 * const es5par = esPar(5): false
 */
export function esPar(numero) {
  // tu codigo aqui :)
  throw new Error("no implementada la funcion!") //eliminar cuando se vaya a implementar
}
