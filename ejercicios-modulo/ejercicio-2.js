//Ejercicio 2: Contar múltiplos de un número en un rango
//Enunciado: Escribe una función que reciba dos números enteros, n (el divisor) y r (el rango máximo), y que cuente cuántos números entre 1 y r son múltiplos de n.

/**
 * @param {number} divisor numero el cual revisaremos en el rango si es que son multiplos
 * @param {number} rangoMaximo es el rango maximo para contar desde 1 
 *
 * @returns {number} cantidad de numeros multiplos encontrados en el rango
 * @example
 * cuantosMultiplosHay(3, 10) = 3; explicacion: hay 3 numeros multiplos de 3 entre el 1 y el 10, seria el 3, 6 y 9. 
 */
export function cuantosMultiplosHay(divisor, rangoMaximo) {
  //aqui tu codigo
  throw new Error("no implementado!")
}
