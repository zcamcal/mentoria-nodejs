/**
 * Encuentra el descuento correspondiente a una categoría
 * 
 * Esta función recibe el nombre de una categoría como parámetro y devuelve el porcentaje de descuento
 * aplicado a esa categoría. Si la categoría proporcionada no existe, la función devuelve 0.
 * 
 * @param categoria - El nombre de la categoría a la que se le desea obtener el descuento. El parámetro es de tipo `string`.
 * 
 * @returns number - El porcentaje de descuento de la categoría proporcionada. Retorna 0 si la categoría no existe.
 * 
 * @example
 * const descuentoElectronica = encontrarDescuentoCategoria("Electrónica");
 * console.log(descuentoElectronica);  // Imprime: 15
 * 
 * @example
 * const descuentoInexistente = encontrarDescuentoCategoria("RopaDeCine");
 * console.log(descuentoInexistente); // Imprime: 0
 */
export function encontrarDescuentoCategoria(categoria: string): number {

}
